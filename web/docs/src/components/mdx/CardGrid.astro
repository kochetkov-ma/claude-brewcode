---
interface Props {
  columns?: 2 | 3;
}

const { columns = 2 } = Astro.props;
---

<div
  class:list={[
    'not-prose grid gap-4 my-6',
    columns === 3
      ? 'grid-cols-1 md:grid-cols-2 lg:grid-cols-3'
      : 'grid-cols-1 md:grid-cols-2',
  ]}
>
  <slot />
</div>
