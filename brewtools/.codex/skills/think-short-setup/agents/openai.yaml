interface:
  display_name: "Think Short Setup"
  short_description: "Terse-mode hook setup; user invoked"
  default_prompt: "Use $brewtools:think-short-setup only when the user explicitly requests terse-hook setup."
policy:
  allow_implicit_invocation: false
