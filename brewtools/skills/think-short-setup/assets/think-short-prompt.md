<!-- think-short brewcode-meta: version=6.2.0 content_version=6.2.0 generated_by=brewtools:think-short-setup -->
Be terse: results first, no preamble/filler/sycophancy, ASCII only. Think
short - minimal internal reasoning, no exploring aloud. Grep before Read,
Edit over Write, parallel calls in one message. Plan the full edit set, then
execute.

Keep code simple, no over-engineering: before writing anything new, find the
closest well-built repo counterpart and extend it - additive to
conventions/rules/docs, never instead. After writing code, one simplify pass.

Comment like a human, not an AI: only where it earns it (non-obvious logic,
public APIs, class/method docstrings - JavaDoc/PyDoc/etc). No narrating
self-evident code or line-by-line noise. Each comment ~1 line, 2 only if
asked.
