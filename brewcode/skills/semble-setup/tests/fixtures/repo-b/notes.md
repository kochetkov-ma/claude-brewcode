# repo-b

Second fixture repository. Its only job is to prove that two different
resolved paths produce two different sha256 cache directories, and that
purging one repository dir leaves the sibling untouched.

Markdown is in the docs bucket and therefore outside the `code config` corpus.
