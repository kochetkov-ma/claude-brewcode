[project]
name = "semble-fixture-repo-a"
version = "0.1.0"
description = "Coverage fixture for the brewcode:semble skill tests"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["src"]

[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"
